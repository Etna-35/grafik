# HANDOFF · Etna «no-money-no-honey» — передача в новую сессию

## 🔴 СВЕЖЕЕ — состояние на 2026-06-21 (читать ПЕРВЫМ; перекрывает устаревшее ниже)

**ГЛАВНОЕ — инфраструктура переехала.** Прод **с Beget на SberCloud/Cloud.ru** (VM `213.171.28.138`, Москва,
whitelisted-IP). Причина: IP Beget блокировался из потребительских/мобильных сетей РФ («белые списки»/ТСПУ),
SberCloud пускается из 14/14 РФ-сетей (проверено Globalping).
- **Подключение:** `ssh -i ~/.ssh/etna_sber_rsa rio35@213.171.28.138` (sudo есть). Docker — только **`sudo docker compose`**.
- **Деплой** (как §4 CLAUDE.md, но новый хост): `scp <файлы> -i ~/.ssh/etna_sber_rsa rio35@213.171.28.138:/opt/etna/app/...`
  → `ssh -i ~/.ssh/etna_sber_rsa rio35@213.171.28.138 "cd /opt/etna/deploy && sudo docker compose build api && sudo docker compose up -d api"`.
  Миграции авто-применяются, фронт запечён в образ (после правок `apps/web` нужен rebuild).
- **Beget (`85.198.68.243`, ssh-алиас `etna-vps`)** — ВКЛючён как FALLBACK (вернуть DNS, если что; данные нетронуты).
  Выключить после пары стабильных дней (бэклог).
- **Caddy:** форс **TLS 1.2** + выкл HTTP/3. **TELEGRAM-ФИКС на VM (обязателен):** `.env` →
  `NODE_OPTIONS=--dns-result-order=ipv4first`; compose сервис `api` → `extra_hosts: ["api.telegram.org:149.154.167.220"]`
  (у VM нет IPv6 + часть IP Telegram режется). При пересоздании контейнера не потерять. Детали — CLAUDE.md §1.
- **DNS:** `lk./api./admin.` → 213.171.28.138 (панель Beget, NS beget); корень `@`/`www` — GitHub Pages, НЕ трогать.

**Фичи этой сессии (всё на проде; миграции 034–037, детали в CLAUDE.md §6–7):**
- 034 **тотемы** (именные награды по бокам куклы на стр. прогресса).
- 035 **КАССА** (`treasury.ts`, owner-only планировщик cash-flow; Фазы 1–3: конверты закуп≤30%/хоз≤5% с переносом,
  копилки под платежи, «свободно сегодня», полоса корзин, авто-подсказки перенести/сплит/урезать, повтор.платежи,
  календарь; **ЗП = датированное обязательство к 10-му числу за прошлый месяц**, ФОТ убран из FCF). Спека — `docs/treasury-planner.md`.
  В Финансах СПРЯТАНЫ P&L и «Постоянные платежи».
- 036 **награда официанта за наличный план** (`cashPlan.ts`: серия 5 смен с планом → бонус 1.5% налом, $×N/🏆×N в ЛК).
- 037 **роль дня** (`schedule_shifts.role_override`: сотрудник вышел другой должностью — выбор роли+ставки в редакторе часов).
- Плюс: подсветка куклы УБРАНА (обводила квадрат у непрозрачных картинок).

**ОТКРЫТЫЕ ХВОСТЫ (следующие шаги):**
1. **6 картинок уровней непрозрачные** (`level-9,13,15,16,17,20`) — вшит тёмный квадрат (остальные 14 ок). Ждём от
   владельца **прозрачные re-export** этих 6 → положить в `apps/web/assets/awards/` (автовырез dark-on-dark не работает).
2. **Telegram-отчёты** — починены (`getMe ok=@EtnaStaffbot`), подтвердить на реальном закрытии смены.
3. **Безопасность TODO:** в чате светились приватный SSH-ключ `rio35` и OWNER_PIN 1833 → сменить ключ и PIN; ротация `.env`.
4. **Касса — осталось:** история ставок по периодам; долги/профицит (приоритеты).
5. **Кальяны earned-vs-paid** (оценено, НЕ построено): отделить начислено/выплачено по кальянам + погашение по дням;
   Фаза 1 = поле «выплачено» в закрытии смены + payroll + ЛК. Бэкфилл: прошлые кальяны пометить оплаченными.
6. **Beget выключить** после стабильности нового сервера.

Детали — в **CLAUDE.md** (авто-загружается: §1 сервер/деплой/Telegram, §6 миграции →037, §7 фичи, §8 камни).
Всё ниже (от 2026-06-11) — историческое.

---

Это стартовый документ для нового агента. Прочитать ПЕРВЫМ, затем **`CLAUDE.md`** (авто-загружается, самый
актуальный: §6 миграции →032, §7 «Новое» со всеми фичами, §8 подводные камни) и **`docs/roadmap-blocks.md`**
(полный статус Блоков 1–4 + Финансы). Этот HANDOFF даёт каркас; детали и свежие фичи — в CLAUDE.md.
Обновлено: 2026-06-11. Ветка `main`, дерево чистое.

> ⚠️ Состояние на 2026-06-11: реализованы и на проде Блоки 1–4 + финансовый фундамент (P&L, прогноз выручки,
> расходы, постоянные платежи), продуктовая матрица с ценами (321 поз.), стоимость заявки + контроль фудкоста,
> ЗП прошлых месяцев, тест-челлендж, выплаты за выбранный месяц и привязка к обязательствам, Telegram-уведомления.
> Осталось из плана: только **хозматрица с ценами** (владелец заполнит `docs/catalog-household.csv`).
> Разделы §5/§9 ниже — исторические; актуальный статус смотри в `docs/roadmap-blocks.md` и CLAUDE.md §7.

---

## 1. Что это и где живёт
Внутренний корпоративный кабинет ресторана-бара Etna (Вологда). ~15 сотрудников, 2–5 онлайн.
НЕ высоконагруженный проект — не усложнять.

- **Прод (живое):** https://lk.no-money-no-honey.ru/ (вход по PIN).
  - `/grafik` график, `/smena` закрытие смены, `/tasks` задачи, `/training` обучение,
    `/zayavka` заявка, `/payroll` выплаты, `/admin` админка.
  - `https://api.no-money-no-honey.ru/api/health` — healthcheck. `https://admin.no-money-no-honey.ru/` — NocoDB.
- **Репозиторий:** `github.com/Etna-35/grafik` (origin, ветка `main`). Репозиторий публичный (см. риск в §8).
- **Сервер:** Beget VPS, IP `85.198.68.243` (сменился с `212.67.14.25` ~2026-06-09; алиас обновлён),
  SSH-алиас **`etna-vps`** (ключ уже на машине пользователя). `ssh etna-vps` идёт по IP — работает даже при лежащем DNS.
  - Код: `/opt/etna/app`. Compose+.env: `/opt/etna/deploy`. Docker Compose: postgres, redis, nocodb, caddy, api.

## 2. Стек и структура
- **Backend:** Node + Fastify + PostgreSQL (`apps/api/src/*.ts`, TypeScript). Точка входа `server.ts`.
  Модули: admin, auth, schedule, shiftClosing, payroll, requisitions, tasks, training, quiz, handovers,
  praises, progress, salesGoals. Импортёры: importTraining, importMenu. Инфра: db, env, security, migrate, seed, types.
- **Frontend:** ВАНИЛЬНЫЙ JS — один файл `apps/web/app.js` (~бол. 3.5к строк, рендер строками шаблонов)
  + `apps/web/styles.css` + `apps/web/index.html`. Никакого фреймворка/сборки фронта.
- **Статика** (`apps/web`, включая `assets/`) отдаётся backend'ом через @fastify/static и **запекается в Docker-образ**.
- Легаси `index.html` в корне репо — старый отдельный «График», НЕ трогать (боевое в `apps/web`).

## 3. Визуальный стиль (бренд RestForm) — строго соблюдать
Тёмная тема, перенесена из соседнего проекта `Etna-35/RestForm` (источник правды по бренду).
- Все цвета — ТОЛЬКО через CSS-переменные в `:root` (`apps/web/styles.css`). Захардкоженных hex не оставлять.
- Палитра: фон `--paper:#0e0e0e`, карточки `--card:#181818`/`--card-2:#222`, текст `--ink:#f0ead8`,
  приглушённый `--ink-soft:#8a8070`, границы `--line:#2e2e2e`. Акцент — антик-золото `--brand:#c8a96e`,
  светлое `--brand-bright:#e2c98a`, **текст на золоте тёмный `--brand-ink:#0e0e0e`** (не белый!).
  Семантика: `--green/--gold/--danger` + `--ok-bg/--ok-line`, `--danger-bg/--danger-line`, `--brand-tint/--brand-line`.
- Шрифты: дисплейный **Unbounded** (`--font-display`: заголовки, золотые метки, суммы, клавиши),
  текст **Golos Text** (`--font`). Подключены в `apps/web/index.html`.
- Mobile-first. Метки минималистичные монохромные, без emoji в UI.

## 4. ПРОЦЕДУРА ДЕПЛОЯ (важно — повторять ровно так)
Изменения едут на сервер копированием файлов + пересборкой образа `api` (фронт запечён в образ!).

1. Правки в коде. Проверка: `npm run check` (tsc) и `node --check apps/web/app.js`.
2. (опц.) Визуальная проверка: Claude Preview / launch.json `web-static` (python http.server по `apps/web`).
   ВНИМАНИЕ: браузер превью кэширует `styles.css` — после правок CSS делать reload и/или cache-bust
   (`link.href='/styles.css?b='+Date.now()`), иначе увидишь старые стили.
3. Коммит в `main`, пуш: `GIT_TERMINAL_PROMPT=0 git push origin main`.
   Если пуш падает с HTTP 400/RPC (большие бинарники): уже выставлен `git config http.postBuffer 524288000`.
4. Залить файлы на сервер (примеры):
   - `scp apps/web/app.js apps/web/styles.css etna-vps:/opt/etna/app/apps/web/`
   - `scp apps/api/src/<файлы>.ts etna-vps:/opt/etna/app/apps/api/src/`
   - новые подпапки: сначала `ssh etna-vps "mkdir -p /opt/etna/app/apps/api/src/data"`
   - миграции: `scp migrations/NNN.sql etna-vps:/opt/etna/app/migrations/`
   - ассеты: `scp apps/web/assets/awards/*.png etna-vps:/opt/etna/app/apps/web/assets/awards/`
5. Пересборка и рестарт:
   `ssh etna-vps "cd /opt/etna/deploy && docker compose build api && docker compose up -d api"`
6. Миграции применяются АВТОМАТИЧЕСКИ при старте контейнера (entrypoint → `node apps/api/dist/migrate.js`),
   затем seed. Проверить лог: `docker compose logs api | grep -i 'Applied migration'`.
7. Импортёры запускать вручную в контейнере после деплоя (НЕ на старте):
   `importMenu` · `importTraining` · `importQuiz` · `importOldClosings` · **`importProductCatalog`** (продуктовый
   каталог с ценами — ЗАМЕНА product-позиций) — напр.
   `ssh etna-vps "cd /opt/etna/deploy && docker compose exec -T api node apps/api/dist/importProductCatalog.js"`.
8. Проверка: `curl -s -o /dev/null -w '%{http_code}' https://api.no-money-no-honey.ru/api/health` → 200.
   БД-проверки: `docker compose exec -T postgres psql -U etna_app -d etna_app -tAc "..."`.

Миграции: только вперёд, нумерация `NNN_name.sql` (последняя — **`032_payroll_history.sql`**). Идемпотентность желательна
(`IF NOT EXISTS`, `DROP CONSTRAINT IF EXISTS`). При правке схемы — обновлять `docs/data-model.md`.

В git-коммитах подпись: `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## 5. Что уже построено (готово, на проде)
- **ЛК/главная:** приветствие + «цель дня» (план выручки = ФОТ_дня/0.23 ×0.8 будни/×1.5 ПТ-СБ; наличный
  план = ФОТ дня), единая подложка с блоками (План выручки / Наличный план / План от коллег).
  Зона **«Заслуги»**: блок Уровня (клик → страница прогресса) + «Задачи на месяц N/N»; ниже похвалы/оценки/«Спасибо».
- **График** (`schedule`): смены, роль-скоуп, оценки (зелёный/жёлтый/красный), выплаты, ДР-метки в календаре, ФОТ.
  Взаимодействие (после разблокировки замком): **одиночный клик** по ячейке — поставить/снять смену;
  **двойной клик** — редактор нестандартных часов (для фикс-ставки — своя сумма); **правый клик** — тоже редактор.
  Долгого тапа больше нет. Календарь раскрыт на всю высоту месяца (вертикально скроллится страница,
  внутри блока — только горизонтальная прокрутка по сотрудникам; шапка дат/имён липкая). Клик по дате →
  окно даты: дедлайн/зарплатный день/оценка + **выплаты за день** (список всех выплат с именами + строка
  добавления «сотрудник + сумма + кнопка» — выбирать сотрудника заранее не нужно).
- **Закрытие смены** (`shiftClosing`): полная форма; переключатель даты ←/→, время МСК + ночное предупреждение
  (00:00–06:00). Отменены пользователем: такси «за наличные», 2 кальянщика (НЕ реализовывать без запроса).
- **Задачи** (`tasks`): описание, дедлайн, **премия** (→ в Выплаты); статус у названия; ролевые задания
  («вся смена» — видно только роли, отметка о выполнении, +10% всем роли). «План на завтра» (`handovers`).
- **Заявка** (`requisitions`): каталог, срочность по позиции, золотые кнопки.
- **Выплаты** (`payroll`): начисления по сменам, кальяны, премии за задачи и за цели продаж.
- **Админка** (`admin`): сотрудники (PIN/роль/ставки/доступы), дата начала + ДР, **удаление (архивация)** с подтверждением.
- **Похвала коллеге** (`praises`) + лента.
- **Цели по продажам** (`salesGoals`): руководитель ставит цель официанту (позиция/кол-во/премия),
  сотрудник жмёт «Продал +1», при достижении руководитель подтверждает → премия в доход. Блок на главной.
- **Прогресс/геймификация** (`progress`): уровень = 100 очков; стаж +30%/мес, задание рук-ля +30,
  ролевое +10 (всем роли), похвала коллеги +10 / рук-ля +20, кнопка «План выполнен» +50 всем.
  Страница прогресса (история «как в MK», скрытый следующий уровень). Иконки уровней — **20 кукол-вуду**
  (`apps/web/assets/awards/level-1..20.png`, `MAX_MASK_LEVEL=20` в app.js).
- **Обучение + Тесты** (`training`, `quiz`): 3 независимых блока (база официанта, меню кухни, меню бара).
  Глава → тест (таймер = вопросов×90с, без подсказок, порог 80%, провал → блок на 2 ч, успех → следующая глава);
  аттестация по модулю. Меню кухни/бара импортированы из ТТК (87 вопросов глав + 26 аттестаций).

## 6. Данные и контент
- **ТТК меню:** Google Sheet `1iu8_-NKfkJqec4-pzZzupTvVt45Omyh8Scumoowb4-o` (CSV-экспорт по gid РАБОТАЕТ),
  xlsx на Drive `1KID9UbtMGyUBGhvPPhspnTnsar0mfjRh`. Выгружено в `apps/api/src/data/menuData.ts` (88 позиций,
  11 листов). Перегенерация: см. ниже «Инструменты».
- **Гайд по интерьеру (PDF):** Google Drive `1r4DkdsVePR2ChRRjjbMfGe7vM-f7Lx3B` — для будущей главы «Интерьер».
- **Логотип:** `apps/web/assets/logo.png` (золотая плитка «ЭТНА»). Награды-куклы: `apps/web/assets/awards/`.
- Контент «Библии» (модуль официанта) импортируется из приватного Google Doc через `importTraining`
  (текст в публичный git НЕ кладём).

## 7. Инструменты на машине пользователя (для контента)
- Python venv с Pillow+openpyxl: `/tmp/maskvenv/bin/python` (может быть удалён — пересоздать:
  `python3 -m venv /tmp/maskvenv && /tmp/maskvenv/bin/pip install Pillow openpyxl`).
- Скачивание Google Drive напрямую: `curl -sL "https://drive.google.com/uc?export=download&id=<FILEID>" -o out`.
- Нарезка спрайтов с прозрачностью/удалением фона делалась Pillow-скриптами (проекции по альфе/белому,
  заливка от углов). Куклы уровней нарезаны 7×3 из спрайта, level-8 убран → 20 шт.

## 8. Подводные камни (НЕ сломать)
- **seed при каждом рестарте обновляет PIN владельца на `OWNER_PIN` из `/opt/etna/deploy/.env`** и гарантирует
  владельца по роли (не по имени — имена НЕ уникальны с миграции 017). Если владелец сменит PIN в приложении —
  деплой его сбросит. TODO: не трогать PIN, если владелец уже есть (оставлено на будущее).
- Имена сотрудников НЕ уникальны (constraint снят миграцией 017). Не возвращать уникальность.
- Текст на золотом фоне — тёмный (`--brand-ink`), не белый.
- Превью-браузер кэширует CSS (см. §4.2).
- Доступ к «Обучению» сейчас у owner/manager/waiter. Повара/бармены НЕ видят меню-модули и аттестации —
  выдать им доступ к разделу через Админку, если потребуется (это решение продукта, не баг).
- Квиз-таймер сейчас клиентский (авто-сабмит по нулю); серверной валидации таймаута нет.
- График: после снятия `max-height` у `.gridwrap` шапка дат/имён липнет к странице (sticky top). Изменения
  графика от 2026-06-07 проверены по синтаксису/логике, но НЕ глянуты вживую с данными — при правках
  поведения липкой шапки/прокрутки смотреть `.gridwrap`, `.schedule-grid thead/.colDate` (sticky) и
  `bindScheduleCells` (click/dblclick/contextmenu).

## 9. Бэклог / что осталось (приоритезировано)
> АКТУАЛЬНО (2026-06-11): почти весь бэклог ниже закрыт. Из согласованного плана осталось **только хозматрица
> с ценами** (`docs/catalog-household.csv` → `importProductCatalog`-аналог). Полный статус — `docs/roadmap-blocks.md`.
> Ниже — исторический бэклог; пункты про Telegram/обучение-роли/выплаты уже реализованы.

Источники: `docs/codex-brief-iteration-2.md` (фазы A–F все сделаны), `docs/codex-brief-quiz-engine.md` (раздел «на будущее»).
- Обучение: вопросы для модуля официанта; доступ меню-модулей поварам/бару; редактор вопросов в админке;
  глава «Интерьер» из PDF; вынос порога/времени в настройки; серверная валидация таймаута.
- Награды: соответствие «уровень→кукла» можно менять (переименование файлов level-N.png + `MAX_MASK_LEVEL`).
- Telegram-уведомления (план выполнен / аттестация / достижение цели) — места в коде заложены.
- Безопасность: репозиторий публичный (в доках IP/адреса) → сделать приватным; перевыпустить токен бота.
- Однократная инициализация PIN владельца (см. §8).
- Маски/иконки: при желании прислать пиксель-перфект PNG-награды с альфой.

## 10. Стартовое сообщение для нового агента (можно скопировать)
> Продолжаем проект Etna (кабинет ресторана). Прочитай **`CLAUDE.md`** (авто-загружен, самый актуальный) и
> **`docs/roadmap-blocks.md`** (статус всех блоков), затем при необходимости `docs/HANDOFF.md`/`AGENTS.md`/
> `DECISIONS.md`/`docs/STATE.md`. Стек: Fastify+Postgres (`apps/api/src/*.ts`, вход `server.ts`), ванильный фронт
> (`apps/web/app.js` ~5к строк + `styles.css`), тёмная тема RestForm (цвета только через CSS-переменные, текст
> на золоте тёмный). Деплой строго по §4 CLAUDE.md/HANDOFF: правки → `npm run check`/`node --check apps/web/app.js`
> → коммит/пуш в main → scp в `/opt/etna/app` (ssh `etna-vps` = IP `85.198.68.243`) →
> `docker compose build api && up -d api` (миграции авто, импортёры вручную) → проверка `/api/health`.
> Не ломать: PIN-seed (по роли), неуникальные имена, тёмный текст на золоте, статику в образе, clearSessionData()
> на login/logout, sentinel scope_id челленджа, household НЕ трогать в importProductCatalog.
> Моя задача: <впиши задачу>.
