# Касса — планировщик денежного потока (owner-only)

Статус: спецификация (2026-06-12). Раздел **только для собственника** (role = `owner`).
Идея и обсуждение: см. чат + макеты. Эта фича — forward-слой поверх существующих Финансов
(`finance.ts`: `predictRevenue`, нормы затрат, ФОТ из графика), но считает **живые деньги на счёте
(cash-flow)**, а не P&L.

---

## 1. Зачем

Помочь владельцу **гарантированно накопить на расчётном счёте к нужной дате** под крупные обязательные
платежи (ЖКХ, аренда, налоги, долги), не залезая в операционные деньги (продукты/напитки/хоз/ФОТ).
Система прогнозирует по средней выручке: хватит ли к дате, сколько откладывать в день, и что делать,
если не хватает (перенести / урезать / сплитовать). Плюс ручные корректировки на форс-мажоры.

## 2. Ключевая концепция — три «корзины»

Каждый рубль прогнозируемой выручки делится на:

1. **Операционка** — продукты, напитки, хоз, ФОТ. Неприкосновенно. **Один % от выручки, задаёт владелец
   вручную** (включает всё: продукты+напитки+хоз+ФОТ). Решено 2026-06-12 — не тянем из P&L finance,
   чтобы старт был простым и предсказуемым.
2. **Резерв** — копится под будущие «точки платежей».
3. **Свободно** — остаток сверх первых двух. Настоящая прибыль/манёвр.

Инвариант: **алгоритм никогда не позволяет платежу залезть в корзину №1.** Это делает подсказки безопасными.

## 3. Сущности (данные)

- **Остаток на счёте** (`treasury_balance`) — якорь прогноза. Снимок: `balance`, `as_of`, `note`.
  Последний снимок = текущая точка отсчёта. Хранится история (для сверки прогноза с фактом).
- **Точки платежей** (`treasury_payments`) — `title`, `amount`, `due_date`, `category`, `priority`,
  `splittable`, `recurring` (none/monthly/...), `status` (planned/paid/moved/split), `parent_id` (для сплита),
  `paid_at`.
- **Корректировки** (`treasury_adjustments`) — внеплановые ± по факту: `amount` (со знаком), `happened_on`,
  `kind` (income/expense), `reason` (форс-мажор / дебиторка / семейное).
- **Настройки** (`treasury_settings`) — `operating_pct` (ручной % операционки), `safety_buffer` (подушка).
- Прогноз выручки **переиспользуем из finance** (`predictRevenue`), операционку — НЕ из finance, а из `operating_pct`.

## 4. Алгоритм (cash-flow прогноз)

1. **Старт:** `B0` = последний снимок остатка. Горизонт = сегодня … `max(due_date)` + буфер.
2. **Дневной свободный поток:** `FCF[d] = прогноз_выручки[d] − операционка[d]`,
   где `операционка[d] = выручка[d] × operating_pct` (ручной %, уже включает ФОТ — сглажен, не по дням выплат).
3. **Покрытие платежей:** платежи по возрастанию `due_date`. Накопленный `FCF` распределяется по очереди;
   `coverage = min(amount, доступно_к_дате)`. Платёж **обеспечен**, если `coverage ≥ amount`,
   иначе `shortfall = amount − coverage`.
4. **Сколько откладывать в день:** `остаток_нужно / дней_до_срока` (ограничено дневным `FCF`).
5. **Свободно сегодня:** `FCF[сегодня] − Σ(рекомендованные отчисления на сегодня)`.
6. **Подсказки при нехватке:** ① ближайшая дата, где платёж обеспечен (перенос);
   ② платежи ниже приоритетом, которые можно подвинуть до срока (на сумму нехватки);
   ③ сплит: сколько реально к сроку + остаток позже.
7. **Профицит:** если резерв перекрывает все платежи + подушку — излишек = «реально свободно»,
   предложение направить на долги.
8. **Корректировки** пересчитывают прогноз; часть платежей может стать «под угрозой».

## 5. API (всё gated `role = owner`)

- `GET  /api/treasury` — текущий остаток, платежи[], корректировки[], расчёт (покрытие по платежам,
  отчисление/день, флаги обеспеченности, разбивка по трём корзинам).
- `POST /api/treasury/balance` — задать снимок остатка.
- `POST|PUT|DELETE /api/treasury/payments[/:id]` — точки платежей.
- `POST /api/treasury/payments/:id/pay` · `.../split` — оплатить / сплитовать.
- `POST|DELETE /api/treasury/adjustments[/:id]` — корректировки.

## 6. UI (макеты согласованы)

Раздел **«Касса»** (плитка только у owner, синтетическая инъекция как у finance/training):
- Остаток на счёте (якорь, редактируемый).
- Полоса трёх корзин (операционка/резерв/свободно).
- **Копилки под платежи** — главный вид: прогресс «накоплено / нужно», % и статус (зелёный/жёлтый/красный).
- **Календарь месяца** — вторичный вид: метки платежей + подсветка напряжённых дней.
- Подсказка «сегодня отложи N · свободно M».
- У красного платежа — кнопки: перенести / сплит / урезать.
- Быстрое добавление: платёж и корректировка (форс-мажор).
- Линия-прогноз из первого макета **отклонена** (менее информативна).

## 7. Этапы

- **Фаза 1 (MVP):** таблицы `treasury_balance` + `treasury_payments`; раздел owner-only; ввод остатка;
  список платежей + копилки + флаг «обеспечен/впритык/риск» по текущему прогнозу `FCF`. Без авто-подсказок
  и корректировок. Цель — **месяц сверять прогноз с реальностью**.
- **Фаза 2:** три корзины + «сколько откладывать в день» + «свободно сегодня» + корректировки
  (`treasury_adjustments`).
- **Фаза 3:** авто-подсказки (перенос/сплит/урезать), повторяющиеся платежи (шаблоны аренды/налогов),
  работа с долгами/профицитом, календарь месяца.

## 8. Открытые вопросы / риски

- **Garbage in, garbage out:** точность зависит от честного ввода остатка и `operating_pct`.
  Поэтому Фаза 1 — сначала проверка прогноза на факте.
- Скоуп-крип в сторону казначейства/ERP — держать раздел простым и owner-only.

### Решено
- Норма операционки — **ручной фикс % выручки** (`operating_pct`), не из finance. ФОТ входит в этот %.
- Визуал: копилки — главный вид, календарь — вторичный; линия-прогноз отклонена.

### Ещё обсудить перед кодом
- Список категорий платежей (ЖКХ, аренда, налог, долг, прочее — или свободный ввод?).
- Подушка безопасности: фикс сумма или % от месячного оборота?
- Прогноз выручки: брать `predictRevenue` из finance или тоже ручная средняя по дням недели?
- Что считаем «долгом» и нужен ли отдельный статус/приоритет для долгов уже в Фазе 1.
