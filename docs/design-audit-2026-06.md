# Аудит дизайна и вёрстки — 2026-06-11

Охват: `apps/web/styles.css` (864 строки, прочитан целиком), `apps/web/app.js` (5036 строк, грепы по
разметке + ключевые render-функции), `apps/web/index.html`, визуальная проверка PIN-экрана в превью
(mobile 375px). Внутренние экраны живьём не смотрелись (нужен API) — строки указаны точно, но перед
правкой каждую цифру перепроверять на месте.

Итог: вёрстка в целом аккуратная и системная (переменные, mobile-first, брейкпоинты 900/760/370).
Найденное — не «сломанный дизайн», а дрейф системы: дубли компонентов, разнобой радиусов/шрифтов,
остатки старой светлой палитры и ~70 строк мёртвого CSS. Всё ниже разбито на мелкие независимые
задачи (Д1–Д12) под дешёвые модели: точные строки, критерии, проверка, что не трогать.

---

## 1. Находки

### P1 — нарушения правил бренда (CLAUDE.md §3)

- **F1. Emoji в UI.** `app.js:1858` — `⚠️ перезакуп …` в контроле фудкоста. Правило: метки
  монохромные, без emoji.
- **F2. Захардкоженные цвета вне `:root`:**
  - `styles.css:241` — `.schedule-grid td.cell.on .h{color:#fff}` (текст на ролевой заливке);
  - `styles.css:313` — `.fixed-pay.on{...;color:#10131a}`;
  - `app.js:4985–4995` — `tintFor()` возвращает rgba-тинты **старой светлой палитры**
    (`rgba(143,36,51,.08)` и т.п.): на тёмном фоне подложки иконок сервисов почти невидимы
    и перебивают корректный `--brand-tint` из CSS (`.navcard .icon`, `styles.css:101`).
- **F3. Контраст белого на ролевых цветах.** `#fff` на `--waiter:#cf875a` ≈ 2.5:1, на
  `--bar:#5ab8a0` ≈ 2.2:1 — плохо читается число часов в ячейке графика. На золоте проект уже
  использует тёмный `--brand-ink` — логично распространить на светлые ролевые заливки.

### P2 — дрейф дизайн-системы (консистентность)

- **F4. Два дублирующих класса заголовка секции:** `.section-title` (`styles.css:97`, margin
  `20px 4px 10px`, 3 использования) и `.sec` (`styles.css:278`, margin `22px 4px 10px`,
  25 использований). Типографика идентична — отличается только верхний отступ.
- **F5. Разнобой border-radius.** На мобиле: 8/9/10/11/12/14/16/18px без системы; блок заявок и
  финансов уже на 8px, а соседние `.panel` 12px / `.day-panel` 16px — на одном экране три радиуса.
  На десктопе ≥900px всё принудительно сведено к 8px двумя селекторами-простынями
  (`styles.css:804`, `:814`) — признак, что шкалы нет. Нужны токены `--r-sm/--r-md/--r-lg`.
- **F6. Шесть реализаций прогресс-бара:** `.bar` (168), `.cost-bar` (503), `.award-bar` (595),
  `.goal-bar` (619), `.oblig-bar` (639), `.training-person-progress i` (694) — высоты 4–10px,
  фон то `--track`, то `--line`, радиусы 4–6px.
- **F7. Девять копий стилей текстового поля:** `styles.css:173` (extra-row), `:330` (field),
  `:377` (payout-add), `:393` (req-search), `:464` (task-reward), `:486` (rlc-bought),
  `:509` (handover textarea), `:756` (`#finAmount` — заодно ID-селектор, антипаттерн),
  `:780` (fin-fixed-row). Почти одинаковые декларации, расходятся в мелочах
  (font-weight 500/600/700, радиус 7–10px).
- **F8. Типографическая шкала расползлась:** 18 разных font-size (9–48px). Подписи 9px
  (`styles.css:157` `.badge`; `:268–269` `.paid`/`.rest` в подвале графика) и 10px с цветом
  `--ink-soft` на тёмном — на грани читаемости на телефоне.
- **F9. Отступы без шкалы:** компенсации `margin:0 2px` vs `0 4px` вразнобой, паддинги карточек
  10–15px, межсекционные 20 vs 22px. (Чинится попутно в Д4/Д5, отдельной задачи нет.)

### P3 — гигиена кода

- **F10. Мёртвый CSS ≈70 строк** (классы не встречаются в `app.js`/`index.html`, проверено грепом):
  `.stats`/`.stat*` (86–96), `.goal-cards`/`.goal`+`span`/`b` (91–94), `.greet-goal` (90),
  `.whorow` (149), весь легаси-блок старой таблицы графика (298–320: `.schedule-wrap`,
  `.schedule-table*`, `.date-cell`, `.mini-mark`, `.shift-cell*`, `.fot-cell`,
  `.employee-totals`), `.schedule-head` (294), `.hi.small` (295), `.month-row` (296),
  `.schedule-summary` (297), `.tenure-chip` (522) + их упоминания в медиа-блоках (808, 846, 848).
  ВНИМАНИЕ: `.score.green/.yellow/.red` (314–316) проверить отдельно — рядом живые `.score-pick.*`.
- **F11. Пустой заголовок как спейсер:** `app.js:269` — `<h2 class="section-title"></h2>` перед
  кнопкой «Выйти» на главной.
- **F12. 29 инлайн-стилей в app.js.** `width:${pct}%` для баров — норм (динамика). Остальное —
  статика, должна жить в CSS: `margin-top:10px/12px` (553, 558, 2551, 2554, 2557, 2559),
  `text-align:left` (557), `width:100%` (1117), `margin:0 2px 12px` (1106), `margin:0 2px 8px` (3997).
- **F13. index.html: нет favicon** (каждая загрузка — 404 на /favicon.ico) **и нет
  `<meta name="theme-color" content="#0e0e0e">`** — адресная строка мобильного браузера не в цвет фона.

### P4 — UX / доступность (без слома стиля)

- **F14. aria-label почти нет:** 3 на весь app.js. Кнопки «назад» (10 шт, `data-action="back"` и
  аналоги), `+` в каталоге заявки (`req-add`), степперы `+/−`, замок графика — без подписи.
- **F15. Мелкие touch-targets:** `.req-cart-x` ≈24×20px (`styles.css:474`), `.score-pick` 24px
  (367), кнопки `.req-stepper` 30px (411), чекбокс закупа 17px (481). Рекомендация ~44px —
  добирать невидимой hit-area, не меняя визуал.
- **F16. Нет focus-стилей** для кнопок/инпутов (есть только `select:focus`, `styles.css:59`) —
  при навигации с клавиатуры (десктоп у руководителя) фокус невидим или дефолтно-синий.
- **F17. 12 системных `alert()/confirm()`** (квиз, план выполнен, обязательства, удаление
  сотрудника) — функционально надёжно, но выпадает из бренда. Менять только по желанию владельца —
  отдельным проектом «фирменный confirm/toast», в этот план не включено.
- **F18. Риск обрезания сумм:** `.day-block b{white-space:nowrap}` (`styles.css:538`) при трёх
  колонках на 360px; overflow не задан. Проверить на узком экране, при переполнении —
  `font-size`-степпинг или `overflow:hidden;text-overflow:ellipsis`.

Отдельно (не дефект): viewport `user-scalable=no` — осознанный компромисс app-like интерфейса,
заодно гасит iOS-зум инпутов с font-size<16px. Не трогать.

---

## 2. План внедрения

Порядок ниже = приоритет. Задачи независимы (можно вразбивку), кроме помеченных зависимостей.
После каждой задачи — общий чек-лист §3 и визуальная проверка превью. Деплой — по §4 CLAUDE.md,
по команде владельца.

| Этап | Задачи | Модель | Риск |
|---|---|---|---|
| A. Баги бренда | Д1, Д2, Д3 | дешёвая | низкий |
| B. Гигиена | Д4, Д5, Д6 | дешёвая | низкий (Д4 — только удаление по грепу) |
| C. Унификация системы | Д7, Д8, Д9 | дешёвая при точной постановке; Д9 — средняя | средний (визуальные сдвиги) |
| D. A11y/полировка | Д10, Д11, Д12 | дешёвая | низкий |

## 3. Общий чек-лист проверки (вставлять в каждую задачу)

```
1. npm run check            # tsc из корня
2. node --check apps/web/app.js
3. Превью web-static (.claude/launch.json), мобильная ширина 375px.
   ВАЖНО: превью кэширует styles.css — в консоли превью выполнить
   document.querySelector('link[rel=stylesheet]').href='/styles.css?b='+Date.now()
4. Грепы из критериев задачи.
5. НЕ коммитить и НЕ деплоить без команды владельца.
```

Глобальные рамки для ВСЕХ задач (из CLAUDE.md §3/§8):
- Цвета — только через CSS-переменные `:root`; текст на золотой заливке — тёмный `--brand-ink`.
- Без emoji в UI; шрифты Unbounded (display) / Golos Text (text) — не переназначать.
- Корневой `index.html` репозитория — ЛЕГАСИ, НЕ трогать (боевое — `apps/web/index.html`).
- Не менять логику app.js (рендер-строки можно, обработчики/данные — нет).
- Номера строк в задачах — ориентир на момент аудита; искать по содержимому, не слепо по номеру.

---

## 4. Задачи

### Д1. Убрать emoji из контроля фудкоста
- **КОНТЕКСТ:** CLAUDE.md §3 (без emoji), этот файл §1/F1.
- **ЦЕЛЬ:** `apps/web/app.js` ~1858: в строке `... · ⚠️ перезакуп +${...} ₽` убрать `⚠️ `
  (текста «перезакуп +N ₽» достаточно; класс `.cost-row.over` уже красит строку в danger).
- **РАМКИ:** менять только этот шаблонный литерал.
- **ГОТОВО:** `grep -c '⚠️' apps/web/app.js` → 0; чек-лист §3.

### Д2. Заменить tintFor() на тёмные тинты палитры
- **КОНТЕКСТ:** §1/F2; `apps/web/app.js` — функция `tintFor` (~4985) и место использования
  `renderServiceCard` (~610: `background:${tintFor(service.code)}`), карта `serviceMeta` (~3).
- **ЦЕЛЬ:** подложки иконок сервисов должны быть тинтами ТЕКУЩЕЙ тёмной палитры, а не старой
  светлой. Сопоставить тинт акценту из `serviceMeta`: brand→`var(--brand-tint)`,
  teal→`rgba(90,184,160,.13)`, gold→`rgba(224,168,74,.13)`, green→`rgba(90,184,122,.13)`,
  ink(admin)→`rgba(240,234,216,.08)`. Новые rgba добавить переменными в `:root`
  (`--teal-tint`, `--gold-tint`, `--green-tint`, `--ink-tint`) и в JS вернуть `var(--…)`.
- **РАМКИ:** не менять `serviceMeta.accent`, иконки, разметку navcard. Также заменить второе
  использование акцентов в admin (`app.js` ~2826) НЕ нужно — там только color.
- **ГОТОВО:** `grep -n 'rgba(143,36,51' apps/web/app.js` → пусто; в превью иконки сервисов имеют
  видимую тонир. подложку (скрин до/после если возможно); чек-лист §3.

### Д3. Тёмный текст на светлых ролевых заливках + убрать hex
- **КОНТЕКСТ:** §1/F2, F3; `apps/web/styles.css` строки ~241 и ~313.
- **ЦЕЛЬ:** 1) `.schedule-grid td.cell.on .h{color:#fff}` → `color:var(--brand-ink)`
  (тёмный #0e0e0e читается на всех четырёх ролевых цветах: контраст ≥7:1 на waiter/bar/cook/dish).
  2) `.fixed-pay.on{...color:#10131a}` → `color:var(--brand-ink)`.
- **РАМКИ:** только эти два правила. НЕ трогать `rgba(...)`-тени/подложки.
- **ГОТОВО:** `grep -nE '#[0-9a-f]{3,6}' apps/web/styles.css | grep -v ':root' | grep -v 'svg'` →
  пусто (data-URI каретки селекта с `%238a8070` — исключение, не трогать); часы в ячейках графика
  читаются (превью недоступно с данными — проверит владелец на проде); чек-лист §3.

### Д4. Удалить мёртвый CSS (~70 строк)
- **КОНТЕКСТ:** §1/F10. Список кандидатов (строки на момент аудита): `.stats`, `.stat`,
  `.stat-praise .v`, `button.stat`, `.stat .k`, `.stat .v` (86–96); `.greet-goal` (90);
  `.goal-cards`, `.goal`, `.goal span`, `.goal b` (91–94); `.whorow` (149); блок 294–320:
  `.schedule-head`, `.hi.small`, `.month-row`, `.schedule-summary`, `.schedule-wrap`,
  `.schedule-table` (все правила), `.date-cell`, `.mini-mark`, `.shift-cell` (все), `.fot-cell`,
  `.employee-totals`; `.tenure-chip` (522); упоминания этих классов в `@media` (808 `.stats`,
  846 `.stats`, 848 `.schedule-summary`).
- **ПОРЯДОК (обязательный):** для КАЖДОГО класса перед удалением выполнить
  `grep -c '<имя-класса>' apps/web/app.js apps/web/index.html` — удалять только при 0.
  ОСТАВИТЬ: `.total-card`, `.import-panel`, `.photo-grid` (используются по 1 разу),
  `.score.green/.yellow/.red` (314–316) — удалять ТОЛЬКО если греп `"score green"`/`score yellow`/
  `score red` по app.js пуст (рядом живые `.score-pick.green` — не перепутать).
- **РАМКИ:** ничего не переименовывать, только удалять целые правила. app.js не трогать.
- **ГОТОВО:** все удалённые имена классов дают 0 в грепе по app.js; превью: главная/PIN без
  визуальных изменений; чек-лист §3.

### Д5. Инлайн-стили app.js → классы
- **КОНТЕКСТ:** §1/F11, F12.
- **ЦЕЛЬ:** 1) `app.js:269` пустой `<h2 class="section-title"></h2>` → `<div class="hub-gap"></div>`,
  в CSS добавить `.hub-gap{height:20px}`. 2) Завести в CSS утилиты: `.mt-10{margin-top:10px}`,
  `.mt-12{margin-top:12px}`, `.w-100{width:100%}`, `.ta-left{text-align:left}`,
  `.hint-block{margin:0 2px 12px}`, `.hint-block-sm{margin:0 2px 8px}` и заменить статические
  инлайны в app.js: строки ~553, 557, 558, 1106, 1117, 2551, 2554, 2557, 2559, 3997.
- **РАМКИ:** инлайновые `style="width:${...}%"` у прогресс-баров и `style="background:${roleColor(...)}"`
  / `border-left-color` / `color:${meta.accent}` — НЕ трогать (динамика). Утилиты добавить одним
  блоком в конец styles.css с комментарием `/* утилиты */`.
- **ГОТОВО:** `grep -c 'style="margin\|style="width:100%\|style="text-align' apps/web/app.js` → 0;
  превью: форма «Спасибо» и «Обязательства» выглядят как раньше; чек-лист §3.

### Д6. favicon + theme-color
- **КОНТЕКСТ:** §1/F13; `apps/web/index.html`.
- **ЦЕЛЬ:** добавить в `<head>`: `<meta name="theme-color" content="#0e0e0e">` и
  `<link rel="icon" href="/assets/logo.png" type="image/png">` (логотип уже есть:
  `apps/web/assets/logo.png`).
- **РАМКИ:** корневой `index.html` репозитория НЕ трогать (легаси). Только `apps/web/index.html`.
- **ГОТОВО:** в превью вкладка получает иконку, запрос favicon не 404; чек-лист §3.

### Д7. Единый заголовок секции: слить .section-title и .sec
- **КОНТЕКСТ:** §1/F4; `styles.css:97` и `:278`; в app.js `.section-title` 3 шт (~265, 269 — после
  Д5 останется меньше), `.sec` 25 шт.
- **ЦЕЛЬ:** один источник правды: правило
  `.sec,.section-title{font-family:var(--font-display);font-size:13px;font-weight:600;letter-spacing:.08em;text-transform:uppercase;color:var(--brand-bright);margin:22px 4px 10px}`,
  старые два правила удалить. (Унифицированный отступ — 22px, как у большинства.)
- **РАМКИ:** app.js не менять (оба класса остаются валидными). Не менять типографику.
- **ГОТОВО:** в styles.css ровно одно правило с `letter-spacing:.08em` для заголовков секций;
  превью главной: заголовок «Сервисы» не сместился более чем на 2px; чек-лист §3.

### Д8. Токены радиусов
- **КОНТЕКСТ:** §1/F5. Зависимость: лучше делать ПОСЛЕ Д4 (меньше правил).
- **ЦЕЛЬ:** добавить в `:root`: `--r-sm:8px;--r-md:12px;--r-lg:16px;--r-pill:999px`. Заменить
  в styles.css: 8/9/10px → `var(--r-sm)`; 11/12px → `var(--r-md)`; 14/16/18px → `var(--r-lg)`;
  999px → `var(--r-pill)`. Радиусы ≤6px (бары, scoreDot, мелкие маркеры) НЕ трогать.
  Десктоп-нормализаторы (`@media(min-width:900px)` строки ~801, 804, 814) оставить, но переписать
  значением `var(--r-sm)`.
- **РАМКИ:** это РЕФАКТОРИНГ ЗНАЧЕНИЙ 1:1 по таблице выше — визуальный сдвиг радиуса на 1–2px
  (9→8, 11→12, 14→16, 18→16) допустим и ожидаем; ничего больше не менять. border-radius в
  inline-SVG и `border-radius:0` не трогать.
- **ГОТОВО:** `grep -cE 'border-radius:(8|9|10|11|12|14|16|18)px' apps/web/styles.css` → 0;
  превью: PIN-клавиши, карточки сервисов, панели — радиусы согласованы; чек-лист §3.

### Д9. Единый компонент прогресс-бара `.meter`
- **КОНТЕКСТ:** §1/F6. Шесть реализаций: `.bar`(168), `.cost-bar`(503), `.award-bar`(595+653 big),
  `.goal-bar`(619), `.oblig-bar`(639), `.training-person-progress i`(694).
- **ЦЕЛЬ:** добавить в CSS компонент:
  `.meter{height:8px;border-radius:5px;background:var(--track);overflow:hidden}`
  `.meter>i{display:block;height:100%;border-radius:5px;background:var(--brand)}`
  `.meter.thin{height:6px}.meter.big{height:10px}.meter.ok>i{background:var(--green)}.meter.danger>i{background:var(--danger)}`.
  В app.js заменить классы в шаблонах: `award-bar`→`meter` (+`big` где `award-bar big`),
  `goal-bar`→`meter`, `oblig-bar`→`meter thin ok`, `cost-bar`→`meter thin ok` (а в строке с
  `.cost-row.over` логика цвета: заменить CSS-правило `.cost-row.over .cost-bar>i` на
  `.cost-row.over .meter>i{background:var(--danger)}`), `training-person-progress i` оставить
  (нестандартная вложенность `i>span` — НЕ трогать), `.bar` в шапке графика (`th.emp .bar`,
  app.js ~4105) оставить — он окрашивается инлайн-фоном роли.
- **РАМКИ:** transition у goal-bar (`width .2s`) сохранить: добавить `.meter>i{transition:width .2s ease}`.
  Старые правила удалить только для реально заменённых классов. Это самая объёмная задача —
  делать по одному компоненту с проверкой превью после каждого.
- **ГОТОВО:** грепы `award-bar|goal-bar|oblig-bar|cost-bar` по app.js → 0; в styles.css осталось
  ≤1 «своя» реализация бара (`.bar` графика + `training-person-progress`); чек-лист §3.

### Д10. aria-label для икон-кнопок
- **КОНТЕКСТ:** §1/F14.
- **ЦЕЛЬ:** добавить `aria-label` в шаблоны app.js: все кнопки «назад» (`class="iconbtn"` c
  `arrowLeftIcon()`, ~10 мест: 431, 542, 667, 705, 1222, 1661, 2386, 2662, 3067, 3097, 3905) →
  `aria-label="Назад"`; `req-add` (+) → `aria-label="Добавить в заявку"`; кнопки степпера
  `req-stepper` → `aria-label="Меньше"` / `aria-label="Больше"`; кнопка замка графика
  (`schedule-lock-btn`) — только если это иконка без текста (проверить, у неё есть текст — тогда пропустить).
- **РАМКИ:** только атрибуты, никакой логики/классов.
- **ГОТОВО:** `grep -c 'aria-label' apps/web/app.js` ≥ 14; чек-лист §3.

### Д11. Touch-targets без смены визуала
- **КОНТЕКСТ:** §1/F15.
- **ЦЕЛЬ:** увеличить зону нажатия, не меняя видимый размер:
  1) `.req-cart-x` (styles.css ~474): `padding:2px 5px` → `padding:10px 12px;margin:-8px -7px` —
     визуально на месте, зона ~40px;
  2) кнопки `.req-stepper button` (~411): `height:30px` → `height:38px` (степпер станет чуть выше —
     допустимо), у `b` соседнего проверить вертикальное выравнивание (`align-items:center` уже есть);
  3) `.score-pick` (~367): 24px → 32px (`width:32px;height:32px`), внутренняя точка `::after`
     остаётся 10px;
  4) чекбокс `.req-line-check input[type=checkbox]` (~481): 17px → 20px.
- **РАМКИ:** только перечисленные правила; сетки строк не менять.
- **ГОТОВО:** превью каталога заявки: строки не разъехались, крестик корзины кликается с запасом;
  чек-лист §3.

### Д12. Focus-стили
- **КОНТЕКСТ:** §1/F16.
- **ЦЕЛЬ:** добавить в styles.css (рядом с базовыми элементами, ~стр. 53–60):
  `button:focus-visible,a:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible,summary:focus-visible{outline:2px solid var(--brand-line);outline-offset:2px}`
  и убрать точечный `select:focus{outline:none;...}` → оставить только `border-color:var(--brand)`
  под `select:focus`.
- **РАМКИ:** именно `:focus-visible` (не `:focus`) — чтобы тач-нажатия не подсвечивались.
- **ГОТОВО:** в превью Tab по PIN-клавиатуре показывает золотую обводку; тап мышью обводку
  не оставляет; чек-лист §3.

---

## 5. Что сознательно НЕ вошло
- Замена `alert/confirm` на фирменные диалоги (F17) — менять поведение, нужно решение владельца.
- Нормализация всех паддингов карточек до токенов (F9) — выгода маленькая, риск визуальной
  регрессии по всем экранам; делать только под визуальным контролем на проде.
- Проверка переполнения `.day-block` на 360px (F18) — нужна живая главная с данными; владельцу
  глянуть на узком телефоне, при обрезании сумм — отдельная мелкая задача (ellipsis).
- Типографические токены `--fs-*` (F8) — после Д1–Д9; самостоятельной ценности мало, а replace
  по 18 размерам — рискован для дешёвой модели. Точечно: поднять 9px → 10px в `.badge` и
  `.paid/.rest` подвала графика можно отдельной микрозадачей.
