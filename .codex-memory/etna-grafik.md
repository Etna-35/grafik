# График смен Etna

Проект: «График смен Etna».

Основной файл приложения: `grafik/index.html` в рабочей папке проекта.

Приложение полностью клиентское: хранит данные в `localStorage`, резервное копирование выполняется через выгрузку и загрузку JSON внутри интерфейса.

Деплой: GitHub Pages.

Правки логики приложения предварительно согласовывать с пользователем.

## Личный кабинет / сервис

Выбранная целевая связка для полноценного ЛК: Postgres + небольшой backend + NocoDB как внутренняя админка руководителя.

Backend должен быть слоем авторизации, прав доступа и бизнес-логики. NocoDB не использовать как публичный интерфейс для сотрудников.

Целевые документы подготовки:

- `docs/service-architecture.md`
- `docs/data-model.md`
- `docs/implementation-roadmap.md`

Текущий GitHub Pages график оставить рабочим до переноса календаря в backend и Postgres.

Для MVP выбран VPS в РФ от Beget. План деплоя: `docs/deployment-beget.md`.

Купленная конфигурация: 2 CPU 3-3.3 GHz, 4 GB RAM, 40 GB NVMe, канал 1 Gbit/s, Ubuntu 24.04, IP `212.67.14.25`. Backup VPS провайдера не заменяет ежедневный `pg_dump` Postgres.

2026-06-03 VPS первично настроен: Docker/Compose, Postgres, Redis, NocoDB, Caddy HTTPS, firewall только `22/80/443`, swap 2 GB, ежедневный backup Postgres в 03:15. Поддомены `lk`, `api`, `admin` смотрят на VPS и отдают 200.

2026-06-03 развернут MVP ЛК: `apps/api` на Fastify/TypeScript, SQL migrations, argon2id PIN, httpOnly cookie sessions, frontend `apps/web`. Первый owner `Руководитель` заходит по PIN, получает сервисы `schedule`, `shift_close`, `tasks`, `payroll`, `admin`. PIN и NocoDB пароль хранятся на сервере в `/opt/etna/secrets/initial-credentials.txt`, не в git.

2026-06-03 начат перенос календаря: добавлен `apps/api/src/schedule.ts`, миграция `002_schedule_import_fields.sql`, API `/api/schedule` и `/api/schedule/import`, экран `/grafik` в ЛК читает Postgres и импортирует JSON-бэкап старого календаря. Тестовый импорт проверен и очищен.

2026-06-03 добавлено редактирование календаря в ЛК через защищенный manager-only API: поставить/снять смену, изменить часы или фиксированную сумму, отметить дедлайн, назначенный день зарплаты, фактическую выплату и зеленую/желтую/красную точку оценки. Выплаты не меняют расчетный дневной ФОТ и план выручки. Интеграционный тест через публичный домен `https://lk.no-money-no-honey.ru/grafik` прошел, тестовые данные удалены. Следующее: управление сотрудниками, PIN и доступами.

2026-06-03 добавлена безопасная версия календаря для обычного сотрудника: общий график виден, но ФОТ, план выручки, ставки и чужие начисления не уходят на клиент. Сотрудник видит свои часы, начислено, выдано, остаток, свои фактические выплаты и свои назначенные дни зарплаты. На VPS создан тестовый сотрудник `Тестовый сотрудник` с доступом только к графику, PIN хранится в `/opt/etna/secrets/initial-credentials.txt`, не в git.

`Закрытие смены` не переносить следующим этапом как есть: старый `RestForm` связан с Google Sheets и перегружен. Подключать в конце или отдельным этапом после детальной переработки под Postgres/backend.

2026-06-03 внешний вид календаря в ЛК возвращен ближе к старому графику: широкая компактная сетка, липкая дата, цветные роли, колонка `Σ`, ФОТ/план справа для руководителя, старые карточки итогов и точки оценок в ячейках. Данные остаются в Postgres/API, старый `localStorage` не возвращать. Оценочные точки теперь видны сотрудникам, но ФОТ, план выручки, ставки и чужие деньги остаются скрыты.

2026-06-03 возвращена механика быстрых кликов из старого графика: для hourly-сотрудников клик по ячейке ставит смену с часами по умолчанию, второй клик снимает; долгий тап или правый клик открывает подробный редактор. Для фиксированной оплаты клик открывает выбор 3000/4000/6000/8000 и поле своей суммы, выбор пресета сразу сохраняет и закрывает редактор. Проверено браузером на публичном домене.

2026-06-03 добавлен внутренний раздел ЛК `/admin` для руководителя: список сотрудников, создание и редактирование карточек, PIN из 4 цифр с argon2id-хешированием, параметры для графика, активность и индивидуальные доступы к сервисам. NocoDB оставлен технической базовой админкой по `https://admin.no-money-no-honey.ru/`, но не как рабочий интерфейс сотрудников.

2026-06-03 для ЛК добавлен desktop-режим: на ширине компьютера убрана искусственная телефонная рамка/округлая подложка, график и `/admin` занимают больше рабочей ширины. Мобильный режим до 900px сохранить для сотрудников.

2026-06-04 в графике админские отметки перенесены на клик по дате: дедлайн, назначенный день зарплаты, фактические выплаты и оценки редактируются через дневную панель с выбором сотрудника. Долгий клик/правый клик по ячейке сотрудника оставить только для нестандартных часов или фиксированной суммы смены. Ссылку на старый график убрали, импорт JSON оставлен внизу как малозаметный fallback.

2026-06-04 реализован модуль `Закрытие смены` в ЛК по адресу `/smena`: новая форма вместо Google Sheets/Apps Script пишет структурированные данные в Postgres (`shift_closings`, расходы, фото, Telegram-отчеты), backend считает кассу/выручку/отклонения/процент плана. Telegram не должен блокировать закрытие смены; пока использовать текстовый fallback, PNG-отчет можно доработать отдельно. Правки логики закрытия смены согласовывать с пользователем.

2026-06-04 в `Закрытии смены` кальянщик должен выбираться явно из активных сотрудников с доп. ролью `Кальянщик`; ставка берется из карточки выбранного сотрудника. `hookah_payout` идет в кассовые расходы закрытия смены и одновременно в личный раздел `/payroll` выбранного кальянщика как уже выданное кальянное начисление, не создавая дополнительный остаток к выплате.

2026-06-04 в графике для рядового сотрудника нельзя показывать деньги и месячные итоги: финансовые суммы должны идти в отдельный раздел `/payroll`. Раздел `Выплаты` строится из тех же источников, что график: `schedule_shifts` для начислений и `payroll_payouts` для фактических выплат. Редактирование графика у руководителя должно быть по умолчанию заблокировано и включаться кнопкой `Разблокировать`.

2026-06-04 раздел `Задачи` по адресу `/tasks`: сотрудник видит только свои задачи и может менять статус `В работе`/`Готово`; по команде показывать только обезличенную статистику без деталей чужих задач. Руководитель/менеджер может назначать задачи сотрудникам.

2026-06-04 раздел `Заявка продуктов` по адресу `/zayavka`: сотрудник создает заявку на продукты/хозтовары. Каталог изначально сидировался из `docs/etna-tovarnaya-matrica.xlsx`, но после ручной чистки и редактирования источником правды стал живой каталог в Postgres; Excel теперь только ранний снимок, его не использовать для повторного сида поверх текущих данных. Backend проверяет роль: повар видит все категории кроме алкоголя и напитков, бармен только алкоголь/напитки/фрукты, руководитель и управляющий видят все и могут менять статусы заявок. Заявка не является закупом, не списывает бюджет и не влияет на расчеты закрытия смены.

2026-06-04 подтянуты свежие ручные коммиты пользователя с GitHub: обновлен `AGENTS.md`, добавлены/прочитаны `DECISIONS.md`, `docs/STATE.md`, `docs/codex-task-template.md`. Перед новыми задачами смотреть актуальное состояние в `docs/STATE.md` и решения в корневом `DECISIONS.md`; ссылка в `AGENTS.md` исправлена на фактический путь `DECISIONS.md`.

2026-06-04 пользователь хочет будущий раздел `Обучение`: обучающие материалы для сотрудников и игровые квизы, чтобы сотрудники лучше запоминали обучение. Брифа, схемы БД и UX для этого раздела пока нет; следующий правильный шаг — подготовить `docs/codex-brief-training.md` с ролями, правами видимости, материалами, вопросами, попытками, прогрессом, очками/статусами и отчетом руководителя.
