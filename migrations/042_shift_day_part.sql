-- Пометка «часть дня» у смены: утренняя/вечерняя (когда смена не на весь день).
-- NULL = обычная смена на весь день (вид не меняется). Только визуальная метка, на расчёты не влияет.
ALTER TABLE schedule_shifts ADD COLUMN IF NOT EXISTS day_part text;
