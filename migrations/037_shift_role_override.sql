-- Per-day переопределение должности: сотрудник может выйти на ДРУГУЮ роль в конкретный день
-- (напр. повар отработал официантом) с возможной иной ставкой. Цвет ячейки — по роли дня.
-- pay_amount уже хранит итог (часы×ставка дня), поэтому отдельной колонки ставки не нужно.
ALTER TABLE schedule_shifts ADD COLUMN IF NOT EXISTS role_override text;
